[project]
name = "idx-scraper"
version = "0.1.0"
description = "Near-real-time IDX (Indonesia Stock Exchange) data scraper for algorithmic research"
requires-python = ">=3.11"
dependencies = [
    "curl_cffi>=0.11",
    "pydantic>=2.7",
    "python-dotenv>=1.0",
    "apscheduler>=3.10",
    "yfinance>=0.2.40",
    "pandas>=2.2",
]

[project.optional-dependencies]
supabase = [
    "psycopg[binary]>=3.1",
]
postgres = [
    "psycopg[binary]>=3.1",
]
api = [
    "psycopg[binary]>=3.1",
    "psycopg-pool>=3.2",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]
dev = [
    "pytest>=8.0",
    "ruff>=0.5",
    "numpy>=1.26",
]

[project.scripts]
idx = "idx_scraper.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/idx_scraper"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
# CLI loads dotenv before imports on purpose
"src/idx_scraper/cli.py" = ["E402"]
# Broad exception catches are deliberate: scraping must survive per-item failures
"src/idx_scraper/*.py" = ["BLE001", "S110"]
# Same reasoning for the one-off backfill/refresh jobs
"scripts/*.py" = ["BLE001"]
